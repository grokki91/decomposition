/**
 * Component for showing date
 * @param {string} date
 * @returns <Date date={'31 июля, среда 02:32'}/>
 */

const Date = ({date}) => {
    return(
        <div>{date}</div>
    );
}

export default Date;