/**
 * Component for showing different blocks under search
 * @param {string} news
 * @param {string} href
 * @param {string} icon
 * @returns <News href={page} icon={1} news={Array(5).fill('Some text of news')}/>
 */

const ShortBlock = () => {
    return(
        <>
        
        </>
    );
}

export default ShortBlock;